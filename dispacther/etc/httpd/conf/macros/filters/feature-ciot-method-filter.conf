<Macro feature-ciot-method-filter>
    # Accept just GET, HEAD and POST requests
    RewriteCond %{REQUEST_METHOD} !^(GET|HEAD|POST)
    RewriteRule ^ - [F]
</Macro>

# vim: ft=apache ts=4 sts=4 sw=4 et
